# Protocolo e host da API → {protocol}://{tenant}.{host}
NEXT_PUBLIC_API_PROTOCOL=http
NEXT_PUBLIC_API_HOST=localhost
# Opcional: força o tenant em localhost (sem subdomínio)
# NEXT_PUBLIC_TENANT=acme
